<%= render 'users/user' %>
<br />

<%= render partial: 'jwt_api_entreprise/user_tokens_list', locals: { subtitle: t('jwt_api_entreprise.index.title', count: @user.jwt_api_entreprise.active.count), tokens: @user.jwt_api_entreprise.active } %>

<%= render partial: 'jwt_api_entreprise/user_tokens_list', locals: { subtitle: t('.archived_token_title', count: @user.jwt_api_entreprise.archived.count), tokens: @user.jwt_api_entreprise.archived } %>

<%= render partial: 'jwt_api_entreprise/user_tokens_list', locals: { subtitle: t('.blacklisted_token_title', count: @user.jwt_api_entreprise.blacklisted.count), tokens: @user.jwt_api_entreprise.blacklisted } %>
