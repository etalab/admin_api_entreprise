Bonjour,

Un nouveau token est disponible dans votre espace client.

Vous pouvez le récupérer en suivant ce lien :

<%= @url_to_token %>

Ce token possède les droits d'accès suivants :
<% @token.scopes.each do |scope| %>
  - <%= scope.name %>
<% end %>

N'oubliez pas, vous pouvez consulter les statistiques d'utilisations propre à ce jeton en temps réel. Voici les statistiques actuellement disponibles:

  - la liste des 50 derniers appels
  - les pourcentages des différents code HTTP sur les dernières 30h/8j
  - le nombre d'appels par API sur les dernières 30h/8j

  Si vous avez besoin d'un nouveau token contactez-nous : <%= mail_to(Rails.configuration.emails_sender_address) %>

Vous recevez cet email car vous êtes renseigné parmi les contacts pour le compte <%= @token.user.email %>. Les accès se font uniquement à partir de cette adresse email.

Cordialement,
L'équipe API Entreprise
